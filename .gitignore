# Quarto's working cache
/.quarto/

# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Rapp.history

# Editors and operating systems
.DS_Store
Thumbs.db
*~
.vscode/

# Note: docs/ (the rendered HTML) is deliberately NOT ignored, because the
# rendered files are what gets pushed to the hosting platform.
